{
  "devDependencies": {
    "@cypress-audit/lighthouse": "^1.2.0",
    "cypress": "^13.15.0",
    "faker": "^5.5.3"
  },

  "scripts": {

    "build:production": "node --version",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "nodemailer": "^6.9.13"
  }
 
}
